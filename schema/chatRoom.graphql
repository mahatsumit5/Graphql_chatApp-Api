# # ChatRoom model
type ChatRoom {
  id: ID!
  messages: [Message!]!
  user: [User!]!
}

type Query {
  getAllChatRooms(contains: String, take: Int, page: Int): GetChatRoomResponse!
  getChatRoomById(id: ID!): ChatRoom!
}
